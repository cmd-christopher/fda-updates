---
status: complete
---

# Quick Task 260423-604: Indications Expanded Default

## Completed
- Changed `default_open` flags in `templates/drug_detail.html` so only `indications_and_usage` is `True`; all other sections set to `False`.
- Rebuilt site with `build.py` — all 530 drug detail pages now render with only section 1 (Indications & Usage) expanded.
- Verified: every page has exactly 1 `<details open>` element.