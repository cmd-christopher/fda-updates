# Quick Task: Default-expand only Indications & Usage on drug detail pages

## Description
On drug detail pages, only the "Indications and Usage" section should be expanded by default. All other PI sections should start collapsed (user clicks to expand).

## Tasks

### Task 1: Update section default_open flags in drug_detail.html
- **File:** `templates/drug_detail.html`
- **Action:** Change the `default_open` boolean in the sections tuple (lines 56-71) so that only `indications_and_usage` has `default_open=True`, all others get `default_open=False`.
- **Verify:** The template renders with only Indications & Usage section having the `open` attribute on its `<details>` element.
- **Done:** `build.py` regenerates site and all detail pages have only section 1 expanded.