---
phase: 03-drug-detail
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - templates/drug_detail.html
  - site/css/custom.css
  - site/js/main.js
  - build.py
autonomous: true
requirements:
  - DETAIL-01
  - DETAIL-02
  - DETAIL-03
  - DETAIL-04
  - DETAIL-05
  - DETAIL-06

must_haves:
  truths:
    - "Clicking a drug row on the index page navigates to a dedicated detail page"
    - "Detail page displays all available structured PI sections from the label data"
    - "Boxed warning appears prominently at the top in a distinct callout style that is never collapsed by default"
    - "Long PI sections are collapsible/expandable for scanability"
    - "Navigation back to index page is always available from the detail page"
    - "Detail page shows approval date, type badge, manufacturer, route, and dosage form"
  artifacts:
    - path: "templates/drug_detail.html"
      provides: "Drug detail page template with boxed warning callout, collapsible sections, metadata bar"
      min_lines: 80
    - path: "site/css/custom.css"
      provides: "Detail page styles: boxed warning callout, section headings, collapsible details, metadata bar, responsive layout"
      contains: ["boxed-warning", "drug-detail", "detail-meta"]
    - path: "site/js/main.js"
      provides: "Detail page accordion/enhancement JS (if needed beyond native <details>)"
  key_links:
    - from: "templates/index.html"
      to: "templates/drug_detail.html"
      via: "href='drugs/{{ drug.slug }}.html'"
      pattern: "drugs/.*\\.html"
    - from: "templates/drug_detail.html"
      to: "site/index.html"
      via: "Link back to '../index.html'"
      pattern: "index\\.html"
    - from: "build.py"
      to: "templates/drug_detail.html"
      via: "env.get_template('drug_detail.html')"
      pattern: "drug_detail"
---

<objective>
Create the drug detail page template with full prescribing information display, prominent boxed warning callout, collapsible sections, metadata bar, and navigation. Also add detail page CSS styling and update `main.js` for any detail-page-specific enhancements.

Purpose: This is the core deliverable of Phase 3 — transforming raw FDA label data into a physician-friendly, scannable prescribing information page.

Output: Complete `drug_detail.html` template, updated `custom.css` with detail page styles, and updated `main.js`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-drug-detail/03-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/phases/03-drug-detail/03-01-SUMMARY.md
@templates/base.html
@site/css/custom.css
@site/js/main.js
@build.py

<interfaces>
<!-- From Plan 01 output — sanitizer function and detail template rendering -->

From build.py (after Plan 01):
```python
def sanitize_html(text):
    """Strip dangerous HTML, preserve safe structural tags.
    Returns Markup object for safe Jinja2 rendering."""
    # Removes: <script>, <style>, <iframe> tags + content
    # Removes: onclick, onerror, onload, other event attributes
    # Removes: style attributes
    # Preserves: <table>, <tr>, <td>, <th>, <p>, <br>, <ul>, <ol>, 
    #            <li>, <b>, <i>, <strong>, <em>, <h1>-<h6>, <sub>, <sup>,
    #            <div>, <span>, <a> (without event/style attrs)

# build.py now renders drug_detail.html per drug:
# template = env.get_template("drug_detail.html")
# html = template.render(drug=drug, last_updated=last_updated)
# writes to site/drugs/{drug['slug']}.html
```

Section display order (per CONTEXT.md D-09):
1. Indications & Usage (always visible if present)
2. Boxed Warning (prominent callout, MUST be `<details open>` — always expanded)
3. Dosage & Administration
4. Dosage Forms & Strengths
5. Contraindications
6. Warnings & Cautions (note: NOT "warnings_and_precautions")
7. Adverse Reactions
8. Drug Interactions
9. Use in Specific Populations
10. Overdosage
11. Description
12. Mechanism of Action
13. Clinical Pharmacology (includes MOA if standalone not present)
14. Clinical Studies
15. Nonclinical Toxicology

Collapsibility rules (per CONTEXT.md D-07):
- Key sections default to EXPANDED: Indications, Boxed Warning, Dosage & Admin, Contraindications
- Long sections default to COLLAPSED: Adverse Reactions, Clinical Studies, Clinical Pharmacology, Nonclinical Toxicology, Use in Specific Populations, Drug Interactions
- Medium sections default to EXPANDED: Dosage Forms, Description, Mechanism of Action, Overdosage
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create drug detail template with full PI rendering, boxed warning, and collapsible sections</name>
  <files>templates/drug_detail.html, site/css/custom.css, site/js/main.js, build.py</files>
  <behavior>
    - Rendered detail page shows all available label sections in FDA-standard order
    - Boxed warning appears in a prominent red/amber callout box, never hidden by default
    - Sections use native `<details>` elements for collapsibility
    - Drugs with null labels show "Prescribing information not yet available" message
    - Metadata bar shows approval date, type badge, manufacturer, route, dosage form
    - "← Back to Drug Approvals" navigation link is always visible
  </behavior>
  <action>
1. **Read all existing files first:**
   - `templates/base.html` — shared layout, CSS variables, badge styles, dark mode toggle, asset links
   - `site/css/custom.css` — current Orchid & Teal styles (216 lines)
   - `site/js/main.js` — dark mode toggle + List.js initialization (82 lines)
   - `build.py` — current state with sanitizer and detail page generation from Plan 01

2. **Replace the placeholder `templates/drug_detail.html`** with the full template:

   The template extends `base.html` and renders a drug detail page. Structure:
   
   ```html
   {% extends "base.html" %}
   
   {% block title %}{{ drug.brand_name or drug.generic_name or "Unknown" }} — FDA Prescribing Information{% endblock %}
   
   {% block content %}
   {% if drug.label %}
     <!-- Full PI page with sections -->
   {% else %}
     <!-- Graceful "not available" page -->
   {% endif %}
   {% endblock %}
   ```
   
   **For the null label case (D-10):** Display a clear message:
   ```html
   <div class="empty-state">
     <h2>Prescribing Information Not Yet Available</h2>
     <p>Full prescribing information for {{ drug.brand_name or drug.generic_name }} 
        is not yet available from the FDA. This sometimes occurs with very recent approvals.</p>
     <p>Check the official source: 
        <a href="https://www.accessdata.fda.gov/scripts/cder/daf/index.cfm?event=overview.process&ApplNo={{ drug.application_number|replace('NDA','')|replace('BLA','')|replace('ANDA','') }}">
           View on Drugs@FDA</a></p>
   </div>
   ```
   
   **Navigation (D-13):** At the top, always visible:
   ```html
   <nav class="detail-nav">
     <a href="../index.html">← Back to Drug Approvals</a>
   </nav>
   ```
   
   **Metadata bar (D-14, D-06):** Below the nav, a summary bar showing:
   - Drug brand name (or generic name) as `<h1>`
   - Type badge: New Drug (teal) or New Indication (orchid) — reuse the `.badge` classes from base.html
   - Approval date (formatted with format_date filter)
   - Manufacturer (from `drug.manufacturer_name` — join list with ", ")
   - Route (from `drug.route[0]` if exists, else "—")
   - Dosage form (from `drug.products[0].dosage_form` if exists, else "—")
   - Generic name (if different from brand name)
   
   **Boxed warning (D-06, DETAIL-03):** If `drug.label.boxed_warning` exists and is non-empty:
   ```html
   <div class="boxed-warning">
     <h3>⚠ Boxed Warning</h3>
     <div class="warning-content">
       {% for paragraph in drug.label.boxed_warning %}
         {{ paragraph|sanitize_html }}
       {% endfor %}
     </div>
   </div>
   ```
   - Uses `<details open>` — ALWAYS expanded (DETAIL-03: "never hidden in a collapsed section by default")
   - Red/amber accent color distinct from Orchid & Teal palette
   - ⚠ emoji prefix for visual prominence
   
   **PI sections (D-08, D-09, DETAIL-02, DETAIL-04):** Define a list of sections in FDA order with display names and default open/closed state. Render each available section as a `<details>` element:
   
   Section configuration (in order):
   ```
   key: indications_and_usage, title: "Indications & Usage", default_open: true
   key: dosage_and_administration, title: "Dosage & Administration", default_open: true
   key: dosage_forms_and_strengths, title: "Dosage Forms & Strengths", default_open: true
   key: contraindications, title: "Contraindications", default_open: true
   key: warnings_and_cautions, title: "Warnings & Precautions", default_open: true
   key: adverse_reactions, title: "Adverse Reactions", default_open: false
   key: drug_interactions, title: "Drug Interactions", default_open: false
   key: use_in_specific_populations, title: "Use in Specific Populations", default_open: false
   key: overdosage, title: "Overdosage", default_open: true
   key: description, title: "Description", default_open: true
   key: mechanism_of_action, title: "Mechanism of Action", default_open: true
   key: clinical_pharmacology, title: "Clinical Pharmacology", default_open: false
   key: clinical_studies, title: "Clinical Studies", default_open: false
   key: nonclinical_toxicology, title: "Nonclinical Toxicology", default_open: false
   ```
   
   For each section, render:
   ```html
   <details {% if default_open %}open{% endif %} class="pi-section">
     <summary><span class="section-number">{{ section_number }}</span> {{ section_title }}</summary>
     <div class="section-content">
       {% for paragraph in drug.label[key] %}
         {{ paragraph|sanitize_html }}
       {% endfor %}
     </div>
   </details>
   ```
   
   Note: Label values are 1-item lists. Use `{% for paragraph in drug.label[key] %}` to iterate — this works whether the list has 1 or N items.
   
   Skip sections not present in the drug's label data. Each `<summary>` shows the section title. The `<details>` element is native HTML — no JS needed for collapse/expand (DETAIL-04).
   
   **Important key name:** Use `warnings_and_cautions` (NOT `warnings_and_precautions`) — this is the actual field name in the openFDA label data per D-03.

3. **Add detail page styles to `site/css/custom.css`:**
   
   Append after the existing styles (do NOT modify existing styles):
   
   - `.detail-nav` — top navigation bar, subtle border-bottom, links styled with orchid color
   - `.drug-detail` — content container for the detail page, max-width for readability
   - `.drug-header` — flex layout for drug name + badge
   - `.detail-meta` — metadata bar (approval date, manufacturer, route, dosage form), subtle background, grid/flex layout, responsive stacking on mobile
   - `.boxed-warning` — PROMINENT callout: `border-left: 4px solid #d32f2f` (red accent), `background: #fff5f5` (light red), darker variant in `[data-theme="dark"]`, generous padding, `margin: 1.5rem 0`
   - `.boxed-warning h3` — bold, red text
   - `.pi-section` — `<details>` styling: border-bottom, `summary` cursor pointer, hover states
   - `.pi-section summary` — font-weight 600, padding, custom triangle marker
   - `.pi-section[open] summary` — subtle background change when expanded
   - `.section-number` — inline, muted color, like "1." before each section title
   - `.section-content` — padding, line-height for medical text readability, max-width for paragraph text
   - Mobile responsive at ≤768px: `.detail-meta` stacks vertically, font sizes adjust, boxed warning gets full width
   - Ensure dark mode variants for all new styles using `[data-theme="dark"]` selectors

4. **Update `build.py`:**
   - Make sure the `sanitize_html` filter is registered with the Jinja2 environment: `env.filters["sanitize_html"] = sanitize_html`
   - The detail page render should pass `drug=drug` and `last_updated=last_updated` to the template
   - Keep the existing index page rendering unchanged

5. **Update `site/js/main.js`:**
   - The detail page does NOT need List.js (only the index page uses it)
   - The `main.js` file's List.js IIFE should continue to check `if (!container) return;` — which it already does with `if (!container) return;` on line 50
   - No changes needed to `main.js` for the detail page — `<details>` elements work natively without JS
   - However, verify that the dark mode toggle still works on detail pages (it should, because `base.html` is shared and the toggle code queries `.theme-toggle` which is in the base template)

6. **Run `python3 build.py`** and verify:
   - `site/index.html` still generates correctly with all 210 drugs
   - `site/drugs/` directory contains `.html` files for each drug
   - A drug with a boxed warning (e.g., METHOTREXATE) shows the prominent callout at the top
   - A drug with null label shows the "not yet available" message
   - Detail page has "← Back to Drug Approvals" link
   - Detail page shows type badge, approval date, manufacturer, route, dosage form in metadata bar
   - `<details>` elements work for collapse/expand
   - Dark mode toggle works on detail pages
   - Mobile layout looks correct at ≤768px
  </action>
  <verify>
    <automated>python3 build.py && ls site/drugs/*.html | wc -l && echo "Build succeeded"</automated>
  </verify>
  <done>
    - `templates/drug_detail.html` renders all available label sections in FDA order with `<details>` collapse/expand
    - Boxed warning appears in prominent red callout, always expanded (`<details open>`)
    - Drugs with null labels show "Prescribing information not yet available" with Drugs@FDA link
    - "← Back to Drug Approvals" link visible at top of every detail page
    - Metadata bar shows: approval date, type badge, manufacturer, route, dosage form, generic name
    - `custom.css` has detail page styles (boxed-warning, pi-section, detail-meta, drug-header, detail-nav)
    - Dark mode works on detail pages
    - Mobile responsive layout at ≤768px
    - All 210 drug detail pages generate successfully
    - `site/index.html` unchanged and still functional
    - `main.js` dark mode toggle works on detail pages (no List.js crash)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FDA label data → HTML rendering | Label content sanitized by Plan 01's sanitizer, rendered with `\| sanitize_html` filter |
| Index page → Detail page navigation | Relative paths (`drugs/{slug}.html` and `../index.html`) — no user input in URLs |
| Drug metadata → Display | All metadata from JSON (not user input) — rendered through Jinja2 autoescaping |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | drug_detail.html | accept | Drug data is public FDA information — no PII |
| T-03-05 | Elevation of Privilege | Drug detail links | mitigate | Links use pre-computed slugs (not user-supplied paths); slug collision handled in build.py |
| T-03-06 | Repudiation | Label content accuracy | accept | Content sourced from FDA, displayed as-is after sanitization; "Not for clinical decision-making" disclaimer in footer |
</threat_model>

<verification>
1. `python3 build.py` exits 0 and creates detail pages in `site/drugs/`
2. A drug with a boxed warning (e.g., methotrexate) shows `<div class="boxed-warning">` in its HTML
3. A drug with null label shows "Prescribing information not yet available" in its HTML
4. `grep -c "boxed-warning" site/drugs/methotrexate.html` returns ≥ 1
5. `grep -c "details" site/drugs/cosentyx.html` returns ≥ 5 (multiple sections with `<details>`)
6. `grep -c "Back to Drug Approvals" site/drugs/cosentyx.html` returns ≥ 1
7. Dark mode toggle appears on detail pages (inherited from base.html)
8. Mobile responsive layout works (check @media queries in custom.css for detail styles)
</verification>

<success_criteria>
- Detail pages display complete, well-structured PI sections for every drug
- Boxed warnings are always visible and prominent (never collapsed by default)
- Long sections are collapsible/expandable
- Navigation back to index is always available
- Key metadata visible on detail pages
- Null label drugs show graceful fallback message
- All Phase 3 success criteria from ROADMAP.md are met
</success_criteria>

<output>
After completion, create `.planning/phases/03-drug-detail/03-02-SUMMARY.md`
</output>